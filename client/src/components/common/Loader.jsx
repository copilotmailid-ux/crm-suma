const Loader = () => (
  <div className="loader-wrapper">
    <div className="loader"></div>
    <span className="loader-text">Retrieving records from database...</span>
  </div>
);

export default Loader;
